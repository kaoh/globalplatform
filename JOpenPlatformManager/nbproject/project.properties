application.args=
build.classes.dir=${build.dir}/classes
build.classes.excludes=**/*.java,**/*.form
# This directory is removed when the project is cleaned:
build.dir=build
# Only compile against the classpath explicitly listed here:
build.sysclasspath=ignore
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
debug.classpath=\
    ${run.classpath}
debug.test.classpath=\
    ${run.test.classpath}
# This directory is removed when the project is cleaned:
dist.dir=dist
dist.jar=${dist.dir}/OpenPlatformManager.jar
dist.javadoc.dir=${dist.dir}/javadoc
jar.compress=false
javac.classpath=\
    ${libs.absolutelayout.classpath}:\
    ${reference.___.jar}
# Space-separated list of extra javac options
javac.compilerargs=-Xlint:deprecation -source 1.4 -Xlint:unchecked
javac.deprecation=false
javac.source=${default.javac.source}
javac.target=${default.javac.target}
javac.test.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}:\
    ${libs.junit.classpath}
javadoc.author=false
javadoc.encoding=
javadoc.noindex=false
javadoc.nonavbar=false
javadoc.notree=false
javadoc.private=false
javadoc.splitindex=true
javadoc.use=true
javadoc.version=false
javadoc.windowtitle=
main.class=org.dyndns.widerstand.openplatformmanager.MainJFrame
manifest.file=manifest.mf
platform.active=default_platform
project.___=../JOpenPlatform
project.JOpenPlatform=../JOpenPlatform
reference.___.jar=${project.___}/dist/JOpenPlatform.jar
reference.JOpenPlatform.jar=${project.JOpenPlatform}/dist/JOpenPlatform.jar
run.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}:\
    ${reference.JOpenPlatform.jar}
# Space-separated list of JVM arguments used when running the project
# (you may also define separate properties like run-sys-prop.name=value instead of -Dname=value
# or test-sys-prop.name=value to set system properties for unit tests):
run.jvmargs=
run.test.classpath=\
    ${javac.test.classpath}:\
    ${reference.JOpenPlatform.jar}:\
    ${build.test.classes.dir}
src.dir=src
src.src.dir=src
test.src.dir=test
test.test.dir=test
